# OpenAPI 3.1 document with nested schema validation errors
# Expected errors: Invalid type for operationId, invalid parameter in
openapi: "3.1.0"
info:
  title: Test API
  version: "1.0.0"
paths:
  /users/{userId}:
    get:
      operationId: 123  # Should be string
      parameters:
        - name: userId
          in: invalid  # Should be path, query, header, or cookie
      responses:
        "200":
          description: Success

