# OpenAPI 3.1 document missing required info.title
# Expected errors: Missing required field "title" at info
openapi: "3.1.0"
info:
  version: "1.0.0"
  description: An API without a title
paths:
  /users:
    get:
      responses:
        "200":
          description: Success

