# Telescope Configuration
#
# This is an example configuration file showing how to:
# - Override built-in rule severities
# - Validate non-OpenAPI files with JSON Schema
# - Use Go plugin rules (auto-discovered from .telescope/plugins/)
#
# File location: .telescope/config.yaml

openapi:
  # Glob patterns for OpenAPI files
  patterns:
    - "openapi/**/*"

# Go plugin binaries in .telescope/plugins/ are auto-discovered.
# You can also reference plugins explicitly by path:
# plugins:
#   - .telescope/plugins/example-rules
#   - /absolute/path/to/another-plugin

# Additional validation for non-OpenAPI files
additionalValidation:
  json-schema-validation:
    patterns:
      - custom/custom-json-schema-*.yaml
    schemas:
      - schema: example-json-schema.json
