// Package graph provides the unified workspace graph engine for Telescope.
// It manages document sources, dependency edges, invalidation cascades,
// and the staged processing pipeline. No LSP or CLI dependencies.
package graph
