openapi: "3.1.0"
info:
  title: Unicode Paths Test
  version: "1.0"
paths:
  /users/{userId}:
    get:
      summary: "Get a user — unicode em-dash test"
      operationId: getUser
      parameters:
        - name: userId
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: "OK ✓"
