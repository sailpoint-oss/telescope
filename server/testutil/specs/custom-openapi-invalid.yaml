openapi: 3.0.0
info:
  title: Test API
  version: 1.0.0
paths:
  /pets:
    get:
      # Missing summary - should trigger custom rule error
      operationId: getPets
      responses:
        "200":
          description: Success
    post:
      summary: Create a pet
      operationId: createPet
      responses:
        "201":
          description: Created
