openapi: 3.1.0
info:
  title: Test API
  version: 1.0.0
paths:
  /users:
    get:
      # Missing operationId - should produce a diagnostic
      summary: Get users
      description: Retrieve a list of users
      responses:
        "200":
          description: OK

