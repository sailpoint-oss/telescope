openapi: 3.1.0
info:
  title: Test API
  version: 1.0.0
paths:
  /users:
    get:
      summary: Get users
      description: Retrieve a list of users
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: invalid-type-value
